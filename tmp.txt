ps aux | grep pipboy


rsync -av \
  --exclude '.venv' \
  --exclude '__pycache__' \
  borys@192.168.50.142:/home/borys/pipboy/ \
  ~/pipboy/


host: ssh-keygen -R 192.168.50.210


sudo apt update
sudo apt full-upgrade -y
sudo apt install -y git python3-venv python3-pip python3-pil mc python3-numpy


cd ~
git clone https://github.com/BorisGujvin/pipboy.git
cd pipboy


cd ~/pipboy
python3 -m venv .venv --system-site-packages
source .venv/bin/activate

// sudo apt install -y python3-numpy

sudo apt install -y locales
sudo locale-gen en_GB.UTF-8 ru_RU.UTF-8
sudo update-locale LANG=en_GB.UTF-8 LC_ALL=en_GB.UTF-8

sudo nano /etc/ssh/sshd_config        # AcceptEnv LANG LC_*

sudo raspi-config // enable SPI


sudo systemctl disable --now avahi-daemon.service
sudo systemctl disable NetworkManager-wait-online.service
sudo touch /etc/cloud/cloud-init.disabled

sudo systemctl disable --now bluetooth.service
sudo systemctl disable --now hciuart.service || true

sudo systemctl mask systemd-rfkill.service systemd-rfkill.socket

sudo systemctl stop polkit.service
sudo systemctl mask polkit.service
